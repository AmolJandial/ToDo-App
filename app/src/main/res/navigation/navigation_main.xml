<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_main"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="com.yepyuno.todolist.presentation.ui.home.FragmentHome"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_fragmentHome_to_fragmentCategory"
            app:destination="@id/fragmentCategory" />
    </fragment>
    <fragment
        android:id="@+id/fragmentCategory"
        android:name="com.yepyuno.todolist.presentation.ui.task.FragmentTask"
        android:label="fragment_category"
        tools:layout="@layout/fragment_task" />
</navigation>